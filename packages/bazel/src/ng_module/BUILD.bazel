load("@build_bazel_rules_nodejs//:index.bzl", "nodejs_binary")

# BEGIN-DEV-ONLY
package(default_visibility = ["//packages/bazel:__subpackages__"])

filegroup(
    name = "package_assets",
    srcs = glob(["*"]),
)

nodejs_binary(
    name = "create_partial_compilation_tsconfig",
    data = [
        "create_partial_compilation_tsconfig.js",
    ],
    entry_point = ":create_partial_compilation_tsconfig.js",
    visibility = ["//visibility:public"],
)
# END-DEV-ONLY
